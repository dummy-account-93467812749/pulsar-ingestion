name: azure-eventhub
type: source
image: "" # Assuming it's a custom-code connector for now
topic: persistent://public/default/azure-eventhub-topic # Replace with actual topic if known, otherwise use a placeholder
configFile: config.sample.yml
secretKeys: [] # Add any secret keys if applicable
parallelism: 1
